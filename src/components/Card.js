import React from 'react';

const Card = ({ className, children }) => <div className="bg-gray-100">{children}</div>;

// <div className={`p-12 rounded-lg border border-solid border-gray-200 ${className}`}>

export default Card;
